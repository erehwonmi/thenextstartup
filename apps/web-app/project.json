{
  "name": "web-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web-app",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project web-app --web",
  "targets": {
    "postbuild": {
      "executor": "nx:run-script",
      "options": {
        "script": "web-app:sitemap"
      }
    }
  }
}
